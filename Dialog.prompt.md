Confirmation / completion overlay. Absolutely positioned — put it inside a `position:relative` screen shell.

```jsx
<Dialog title="Treino concluído" message="Você queimou 381 kcal em 45 minutos." actionLabel="Ver resumo" secondaryLabel="Fechar" />
```