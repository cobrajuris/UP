Catalogue tile. Two per row on a phone, 10px gutter.

```jsx
<WorkoutCard title="Peito em casa (sem equipamento)" duration="45 min" level="dificil" saved onSave={alternar} />
```

A protection gradient runs from transparent at 35% to near-black at the bottom so the title always clears 4.5:1.