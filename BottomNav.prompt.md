Primary navigation. Four destinations maximum — the FAB owns the middle slot.

```jsx
<BottomNav active="inicio" onSelect={ir} onFab={abrirRegistro} />
```

Floats 10px above the gesture bar with the screen margin on each side.