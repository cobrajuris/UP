Circular icon control.

```jsx
<IconButton icon="arrow-left" label="Voltar" variant="glass" />
<IconButton icon="bookmark" label="Salvar treino" active />
```

Never smaller than 44px in the app. Over a photo always use `glass`.