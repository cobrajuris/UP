The single lime FAB. It always means "registrar" (log something: workout, meal, water, weight).

```jsx
<Fab onClick={abrirRegistro} />
```

Rendered by `BottomNav` automatically — only place it manually on screens without the nav bar.