Text input. The field is a light pill on the dark screen — high contrast is intentional.

```jsx
<TextField label="E-mail" placeholder="mariana@email.com" type="email" />
<TextField label="Senha" type="password" error="Senha incorreta" />
```